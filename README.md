# Aurum Vault Frontend (Next.js)

This is the Next.js (App Router) version of the Aurum Vault frontend.

## Environment

Create `.env.local`:

- `NEXT_PUBLIC_API_BASE_URL` (example: `https://api.aurumvault.org/api`)

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run start`
